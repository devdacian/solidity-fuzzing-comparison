# no eth required
balanceContract: 0

# Allow fuzzer to use public/external functions from all contracts
allContracts: false

# using assertion mode
testMode: "assertion"

# record fuzzer coverage to see what parts of the code
# fuzzer executes
corpusDir: "./test/06-rarely-false/coverage-echidna-basic"